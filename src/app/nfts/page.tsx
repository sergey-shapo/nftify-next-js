import NftList from "@/components/NftList/NftList";

const NftsPage = (): React.ReactElement => {
  return (
    <>
      <h1>NFTS next generation</h1>
      <NftList />
    </>
  );
};

export default NftsPage;
